
---

## 维度之间的关系

### 典型的张量形状
在实际计算中，数据的形状通常是：

```python
# 输入数据
[batch_size, sequence_length, hidden_dim]
[4, 512, 4096]
# 意思：4个句子，每个最多512个词，每个词用4096维向量表示

# 注意力计算
[batch_size, num_heads, sequence_length, head_dim]
[4, 32, 512, 128]
# 意思：4个句子，32个注意力头，512个词，每个头处理128维
# 注意：num_heads × head_dim = hidden_dim (32 × 128 = 4096)
```

---

## 为什么需要这么多维度？

### 1. 隐藏维度大 → 表达能力强
就像用更多的特征来描述一个事物，能捕捉更细微的差别。

### 2. 序列长度大 → 记忆力好
能记住更长的上下文，理解更复杂的逻辑。

### 3. 层数多 → 理解更深
每一层都在前一层的基础上进行更深入的理解。

### 4. 注意力头多 → 视角多
从不同角度理解同一段文本，更全面。

---

## 实际例子：LLaMA-7B 的维度配置

```python
{
    "hidden_dim": 4096,        # 隐藏维度
    "num_layers": 32,          # 层数
    "num_heads": 32,           # 注意力头数
    "head_dim": 128,           # 每个头的维度 (4096 / 32)
    "ffn_dim": 11008,          # 前馈网络维度 (约4倍)
    "vocab_size": 32000,       # 词表大小
    "max_seq_len": 2048        # 最大序列长度
}
```

### 计算一下参数量
```
嵌入层：32000 × 4096 = 131M
每层Attention：4096 × 4096 × 4 = 67M
每层FFN：4096 × 11008 × 2 = 90M
每层总计：67M + 90M = 157M
32层：157M × 32 = 5024M
总参数：131M + 5024M ≈ 5.2B ≈ 7B（加上其他小参数）
```

---

## 总结

| 维度名称 | 通俗理解 | 典型值 | 影响 |
|---------|---------|--------|------|
| 隐藏维度 | 用多少个数字表示一个词 | 768-8192 | 表达能力 |
| 序列长度 | 一次能处理多少个词 | 512-200K | 上下文记忆 |
| 批次大小 | 同时处理多少个请求 | 1-128 | 吞吐量 |
| 注意力头数 | 多少个理解视角 | 8-96 | 理解全面性 |
| 层数 | 处理多少轮 | 12-96 | 理解深度 |
| 词表大小 | 认识多少个词 | 30K-250K | 语言覆盖 |
| FFN维度 | 中间计算空间 | 隐藏维度×4 | 计算复杂度 |

**核心思想**：维度越大，模型越强大，但也需要更多的计算资源和内存！
