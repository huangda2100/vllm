# 多头注意力机制详解

## 什么是多头注意力？

### 通俗理解

**多头注意力 = 多个专家同时分析同一个问题**

想象你要理解一句话："苹果公司发布了新产品"

**单头注意力（1个专家）**：
- 只有1个人分析，可能只关注到"苹果公司"和"新产品"的关系

**多头注意力（8个专家）**：
- 专家1：关注"苹果公司"是主语
- 专家2：关注"发布"是动作
- 专家3：关注"新产品"是宾语
- 专家4：关注整体是新闻事件
- 专家5：关注时态（过去式）
- 专家6：关注情感（中性）
- 专家7：关注领域（科技）
- 专家8：关注其他细节

最后把8个专家的分析结果综合起来，得到全面的理解。

---

## 为什么是"多头"？

### 1. 单头的局限性

假设隐藏维度是4096，如果只用1个注意力头：
- 这4096维要同时捕捉所有信息
- 语法、语义、情感、逻辑...全部混在一起
- 很难学习到清晰的模式

### 2. 多头的优势

把4096维分成32个头，每个头128维：
- 每个头专注于某一类特征
- 头1可能学会关注语法
- 头2可能学会关注实体关系
- 头3可能学会关注情感
- ...

**类比**：
```
单头 = 一个人做所有工作
     → 什么都懂一点，但都不精通

多头 = 团队分工
     → 每个人专注一个领域，最后汇总
     → 效率更高，效果更好
```

---

## 多头注意力的计算过程

### 步骤1：分头

```python
# 输入
hidden_states: [batch_size, seq_len, hidden_dim]
例如：[4, 512, 4096]

# 分成32个头
num_heads = 32
head_dim = 4096 / 32 = 128

# 重塑形状
reshaped: [batch_size, seq_len, num_heads, head_dim]
         [4, 512, 32, 128]

# 转置以便并行计算
transposed: [batch_size, num_heads, seq_len, head_dim]
           [4, 32, 512, 128]
```

### 步骤2：每个头独立计算注意力

```python
# 对每个头分别计算 Q, K, V
for head in range(32):
    Q_head = Linear(hidden_states)  # [4, 512, 128]
    K_head = Linear(hidden_states)  # [4, 512, 128]
    V_head = Linear(hidden_states)  # [4, 512, 128]

    # 计算注意力分数
    scores = Q_head @ K_head.T / sqrt(128)  # [4, 512, 512]
    attention = softmax(scores)

    # 加权求和
    output_head = attention @ V_head  # [4, 512, 128]
```

### 步骤3：拼接所有头的输出

```python
# 32个头的输出
head_outputs: [batch_size, num_heads, seq_len, head_dim]
             [4, 32, 512, 128]

# 拼接
concatenated: [batch_size, seq_len, num_heads * head_dim]
             [4, 512, 4096]
```

### 步骤4：输出投影

```python
# 最后通过一个线性层
output = Linear(concatenated)  # [4, 512, 4096]
```

---

## 具体例子：理解"我喜欢吃苹果"

假设有4个注意力头：

### 头1：关注主谓关系
```
"我" 对 "喜欢" 的注意力权重：0.8
"我" 对 "吃" 的注意力权重：0.1
"我" 对 "苹果" 的注意力权重：0.1
→ 学到："我"是"喜欢"的主语
```

### 头2：关注动宾关系
```
"吃" 对 "苹果" 的注意力权重：0.9
"吃" 对 "我" 的注意力权重：0.05
"吃" 对 "喜欢" 的注意力权重：0.05
→ 学到："苹果"是"吃"的宾语
```

### 头3：关注情感
```
"喜欢" 对整句话的注意力：均匀分布
→ 学到：这是一个表达正面情感的句子
```

### 头4：关注语序
```
每个词主要关注相邻的词
→ 学到：词的顺序和语法结构
```

最后把4个头的理解综合起来：
- 这是一个主谓宾结构的句子
- 表达了说话人的喜好
- 情感是正面的
- 语法结构正确

---

## 多头注意力的数学表示

### 完整公式

```python
# 对每个头 i (i = 1, 2, ..., num_heads)
Q_i = X @ W_Q_i  # [seq_len, head_dim]
K_i = X @ W_K_i  # [seq_len, head_dim]
V_i = X @ W_V_i  # [seq_len, head_dim]

# 计算注意力
Attention_i = softmax(Q_i @ K_i.T / sqrt(head_dim)) @ V_i

# 拼接所有头
MultiHead = Concat(Attention_1, Attention_2, ..., Attention_n)

# 输出投影
Output = MultiHead @ W_O
```

### 参数量计算（以LLaMA-7B为例）

```python
hidden_dim = 4096
num_heads = 32
head_dim = 128

# Q, K, V 投影矩阵
QKV_params = 3 × (hidden_dim × hidden_dim)
           = 3 × (4096 × 4096)
           = 50,331,648 参数

# 输出投影矩阵
O_params = hidden_dim × hidden_dim
         = 4096 × 4096
         = 16,777,216 参数

# 总计
total = 50M + 16M = 67M 参数（每层）
```

---

## 为什么不是越多头越好？

### 头数的权衡

**太少（如2个头）**：
- 表达能力不足
- 无法捕捉多样的模式

**太多（如256个头）**：
- 每个头的维度太小（4096/256=16维）
- 16维表达能力太弱
- 计算开销大

**合适的选择（32个头）**：
- 每个头128维，表达能力足够
- 32个头能捕捉足够多的模式
- 计算效率较高

### 常见配置

| 模型 | 隐藏维度 | 头数 | 每头维度 |
|------|---------|------|---------|
| BERT-base | 768 | 12 | 64 |
| BERT-large | 1024 | 16 | 64 |
| GPT-2 | 768 | 12 | 64 |
| GPT-3 | 12288 | 96 | 128 |
| LLaMA-7B | 4096 | 32 | 128 |
| LLaMA-70B | 8192 | 64 | 128 |

**规律**：每头维度通常是64或128，然后根据总维度决定头数。

---

## vLLM中的多头注意力实现

### 代码示例

```python
class Attention(nn.Module):
    def __init__(self, config):
        self.hidden_size = 4096
        self.num_heads = 32
        self.head_size = 128  # 4096 / 32

        # QKV投影（融合成一个矩阵）
        self.qkv_proj = ColumnParallelLinear(
            self.hidden_size,
            3 * self.hidden_size,  # Q, K, V
        )

        # 注意力计算（使用优化的PagedAttention）
        self.attn = PagedAttention(
            hidden_size=self.hidden_size,
            head_size=self.head_size,
            total_num_heads=self.num_heads,
        )

        # 输出投影
        self.o_proj = RowParallelLinear(
            self.hidden_size,
            self.hidden_size,
        )

    def forward(
        self,
        positions: torch.Tensor,
        hidden_states: torch.Tensor,
        kv_cache: torch.Tensor,
        attn_metadata: AttentionMetadata,
    ) -> torch.Tensor:
        # QKV投影
        qkv, _ = self.qkv_proj(hidden_states)
        q, k, v = qkv.split([self.q_size, self.kv_size, self.kv_size], dim=-1)

        # 应用RoPE位置编码
        q, k = self.rotary_emb(positions, q, k)

        # 更新KV缓存
        k_cache, v_cache = kv_cache[0], kv_cache[1]
        attn_metadata.slot_mapping.copy_(k, k_cache)
        attn_metadata.slot_mapping.copy_(v, v_cache)

        # 多头注意力计算（使用PagedAttention优化）
        attn_output = self.attn(
            q, k_cache, v_cache, attn_metadata
        )

        # 输出投影
        output, _ = self.o_proj(attn_output)
        return output
```

### 优化技术

vLLM 对多头注意力的优化：

1. **QKV融合**：将3个矩阵乘法融合成1个
2. **PagedAttention**：优化KV缓存的内存管理
3. **Flash Attention**：减少内存访问次数
4. **张量并行**：将多个头分配到不同GPU

---

## 总结

### 核心要点

1. **多头 = 多角度理解**
   - 每个头关注不同的语义关系
   - 综合多个头得到全面理解

2. **为什么需要多头？**
   - 单头能力有限，无法同时捕捉多种模式
   - 多头可以并行学习不同的特征

3. **头数的选择**
   - 通常32个头（LLaMA）或12个头（BERT）
   - 每头维度64-128
   - 不是越多越好

### 类比总结

| 概念 | 类比 |
|------|------|
| 单头注意力 | 一个人做所有工作 |
| 多头注意力 | 团队分工协作 |
| 每个头 | 团队中的专家 |
| 拼接输出 | 汇总各专家的报告 |
| 输出投影 | 整理成最终报告 |

### 实际意义

理解多头注意力，就能理解：
- 为什么Transformer比RNN强：能同时关注多种关系
- 为什么大模型需要大量计算：32个头并行计算
- 为什么vLLM能优化：通过融合和并行加速多头计算

**一句话总结**：多头注意力就像一个专家团队，每个专家从不同角度分析问题，最后综合所有专家的意见得出结论。
